/* XOR (^ in C++) is a bitwise operation.
For each bit position:
0 ^ 0 = 0
1 ^ 1 = 0
0 ^ 1 = 1
1 ^ 0 = 1
So XOR gives 1 if the bits are different, 0 if they’re the same. */
//x ^ x = 0 (any number XORed with itself cancels out).
//x ^ 0 = x.
